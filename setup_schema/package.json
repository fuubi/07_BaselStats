{
  "name": "setup_schema",
  "version": "0.0.1",
  "description": "Setup the schema of the indexed documents",
  "author": "Denis Augsburger",
  "main": "setup.js",
  "scripts": {
    "start": "babel-node setup.js --presets es2015,stage-2",
    "debug": "babel-node-debug setup.js --presets es2015,stage-2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "elasticsearch": "13.3.1",
    "winston": "~2.1.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-stage-2": "^6.24.1"
  }
}
